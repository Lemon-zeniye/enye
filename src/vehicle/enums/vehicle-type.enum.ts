export enum VehicleType {
  TRUCK = 'truck',
  TRAILER = 'tailer',
  VAN = 'van',
  OTHER = 'other',
}

export enum Unit {
  TONS = 'tons',
  KG = 'kg',
  LBS = 'lbs',
}

export enum VehicleStatus {
  AVAILABLE = 'Available',
  IN_TRANSIT = 'In_Transit',
  INACTIVE = 'Inactive',
  RESERVED = 'Reserved',
}
